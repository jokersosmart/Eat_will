# 開發驗證筆記

## 智慧建議前端請求

2026-08-12 的預覽驗證已確認，「取得知識庫智慧建議」按鈕的 React 事件會正確發送 `POST /api/trpc/advice.generate?batch=1`。觸發後 3 秒仍為載入狀態，表示前端互動與 tRPC 請求路徑均正常；後續驗證重點是確認模型回覆能在預覽代理容許時間內完成，並渲染智慧建議、局後復盤摘要與依據條目。

更新伺服器端八秒逾時備援後，重新載入預覽並填寫目標再觸發智慧建議，按鈕已切換為載入中狀態；下一步將確認載入結束後的內容卡渲染。
